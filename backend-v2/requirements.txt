# Colab / Kaggle notebooks already ship torch + torchvision + numpy + pillow.
# Install the rest with:  pip install -r backend-v2/requirements.txt
timm>=1.0.9
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
kaggle>=1.6
huggingface-hub>=0.23
pytest>=8.0

# --- video (backend-v2) ---
# mediapipe pins its own protobuf/numpy range and is the most fragile dependency
# here; it is only needed to turn pixels into landmarks, so training on PopSign
# (already landmarks) and the whole test suite run without it.
opencv-python-headless>=4.9
mediapipe>=0.10.9
pandas>=2.0
pyarrow>=15.0
